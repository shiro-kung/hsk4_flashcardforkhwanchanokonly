
let index=0;let flipped=false;const card=document.getElementById("card");const frontWord=document.getElementById("frontWord");const backPinyin=document.getElementById("backPinyin");const backMeaning=document.getElementById("backMeaning");const progress=document.getElementById("progress");const wordList=document.getElementById("wordList");function renderCard(){const w=words[index];card.classList.toggle("flipped",flipped);frontWord.textContent=w.word;backPinyin.textContent=w.pinyin;backMeaning.textContent=w.meaning;progress.textContent=`คำที่ ${index+1} / ${words.length}`;}function flipCard(){flipped=!flipped;renderCard();}function nextCard(){index=(index+1)%words.length;flipped=false;renderCard();}function prevCard(){index=(index-1+words.length)%words.length;flipped=false;renderCard();}function shuffleCards(){for(let i=words.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[words[i],words[j]]=[words[j],words[i]];}index=0;flipped=false;renderCard();renderList();}function toggleList(){wordList.classList.toggle("hidden");}function renderList(){wordList.innerHTML=`<table><thead><tr><th>#</th><th>จีน</th><th>พินอิน</th><th>ไทย</th></tr></thead><tbody>${words.map((w,i)=>`<tr><td>${i+1}</td><td>${escapeHtml(w.word)}</td><td>${escapeHtml(w.pinyin)}</td><td>${escapeHtml(w.meaning)}</td></tr>`).join("")}</tbody></table>`;}function escapeHtml(text){return String(text).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}document.addEventListener("keydown",e=>{if(e.key==="ArrowRight")nextCard();if(e.key==="ArrowLeft")prevCard();if(e.key===" "||e.key==="Enter")flipCard();});renderCard();renderList();
